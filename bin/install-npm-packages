#!/usr/bin/env zsh

source "$(dirname "$0")/script-helpers"

msg "Global npm packages"

npm update -g

PACKAGES=(
	coffee-script
	coffeelint
	jsonlint
	js-yaml
	meteorite
	wscat
	react-native-cli
	)

npm install -g ${PACKAGES[@]}