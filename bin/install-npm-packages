#!/usr/bin/env zsh

source "$(dirname "$0")/script-helpers"

msg "Global npm packages"

npm update -g

PACKAGES=(
	coffee-script
	coffeelint
	jsonlint
	js-yaml
	meteorite
	wscat
	)

npm install -g ${PACKAGES[@]}