#!/usr/bin/env zsh

source "$(dirname "$0")/script-helpers"

msg "Install node.js versions"

NODES=(
  'iojs-v2.3.3'
  )

for item in "${NODES[@]}"
do
    nvm install $item
done

nvm alias default iojs-v2.3.3
